const vAllowedKeys = require('./validate-allowed-keys');
const vKeyValues = require('./validate-key-vals');
const vRequiredKeys = require('./validate-required-keys');

const vHelpers = {
    vAllowedKeys,
    vKeyValues,
    vRequiredKeys,
};

module.exports = vHelpers;